
%p Title: #{@blog.title}
= link_to "Edit"
= link_to "Back", root_path
%p Listing entries
%table.blogs_content
	%thead
		%tr
			%th Title
			%th Body
	%tbody
		- @blog.entries.each do |entry|
			%tr
				%td= entry.title
				%td= entry.body
				%td= link_to "Show", blog_entry_path(@blog, entry)
				%td= link_to "Edit", edit_blog_entry_path(@blog, entry)
				%td= link_to "Destroy", blog_entry_path(@blog, entry), method: :delete
= link_to "New Entry", new_blog_entry_path(@blog)